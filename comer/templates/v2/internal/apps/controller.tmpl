/*
generated by comer,https://github.com/imoowi/comer
Copyright © 2023 jun<simpleyuan@gmail.com>
*/
package controllers

import (
	"errors"
	"github.com/gin-gonic/gin"
	"github.com/gin-gonic/gin/binding"
	"github.com/imoowi/comer/interfaces"
	"github.com/imoowi/comer/utils/response"
	"{{.ModuleName}}/internal/models"
	"{{.ModuleName}}/internal/services"
	"net/http"
	"gorm.io/gorm"
	"github.com/spf13/cast"
)

// @Summary	分页列表(pagelist)
// @Tags	{{.SwaggerTags}}
// @Accept	application/json
// @Produce	application/json
// @Param	Authorization	header		string				true	"Bearer 用户令牌"
// @Param   {object}             query                 models.{{.ModelName}}Filter  false "query参数"
// @Success 200                        {object} response.PageListT[models.{{.ModelName}}] "成功"
// @Failure 400                        "请求错误"
// @Failure 401                        "token验证失败"
// @Failure 500                         "内部错误"
// @Router		/api/{{.controllerName2Dash}}s [get]
func {{.ControllerName}}PageList(c *gin.Context) {
	var filter interfaces.IFilter = &models.{{.ModelName}}Filter{}
	err := c.ShouldBindQuery(&filter)
	if err != nil {
		response.Error(c, err.Error(), http.StatusBadRequest)
		return
	}
	
	page := c.DefaultQuery(`page`, `1`)
	filter.SetPage(cast.ToInt64(page))
	pageSize := c.DefaultQuery(`pageSize`, `20`)
	filter.SetPageSize(cast.ToInt64(pageSize))
	if filter.GetPageSize() > 1000 {
		response.Error(c, `每一页不能超过1000条记录`, http.StatusBadRequest)
		return
	}
	result, err := services.{{.ServiceName}}.PageList(c, &filter)
	if err != nil {
		if errors.Is(err, gorm.ErrRecordNotFound) {
			response.Error(c, err.Error(), http.StatusNotFound)
			return
		}
		response.Error(c, err.Error(), http.StatusBadRequest)
		return
	}
	response.OK(c,result)
}

// @Summary	详情(one)
// @Tags		{{.SwaggerTags}}
// @Accept		application/json
// @Produce	application/json
// @Param		Authorization	header	string	true	"Bearer 用户令牌"
// @Param		id				path	int		true	"id"
// @Success	200 						{object} models.{{.ModelName}} "成功"
// @Failure 400                        "请求错误"
// @Failure 401                        "token验证失败"
// @Failure 500                         "内部错误"
// @Router		/api/{{.controllerName2Dash}}s/{id} [get]
func {{.ControllerName}}One(c *gin.Context) {
	id := c.Param(`id`)
	if id == `` {
		response.Error(c,`pls input id`, http.StatusBadRequest)
		return
	}

	one, err := services.{{.ServiceName}}.One(c, cast.ToUint(id))
	if err != nil {
		if errors.Is(err, gorm.ErrRecordNotFound) {
			response.Error(c, err.Error(), http.StatusNotFound)
			return
		}
		response.Error(c, err.Error(), http.StatusBadRequest)
		return
	}
	response.OK(c,one)
}

// @Summary	新增(add)
// @Tags		{{.SwaggerTags}}
// @Accept		application/json
// @Produce	application/json
// @Param		Authorization	header	string	true	"Bearer 用户令牌"
// @Param 		{object} body  models.{{.ModelName}} true "body"
// @Success	200
// @Failure 400                        "请求错误"
// @Failure 401                        "token验证失败"
// @Failure 500                         "内部错误"
// @Router		/api/{{.controllerName2Dash}}s [post]
func {{.ControllerName}}Add(c *gin.Context) {
	model := &models.{{.ModelName}}{}
	err := c.ShouldBindBodyWith(&model,binding.JSON)
	if err != nil {
		response.Error(c, err.Error(), http.StatusBadRequest)
		return
	}
	newId, err := services.{{.ServiceName}}.Add(c,  model)
	if err != nil {
		response.Error(c, err.Error(), http.StatusBadRequest)
		return
	}
	response.OK(c,newId)
}

// @Summary	更新(update)
// @Tags		{{.SwaggerTags}}
// @Accept		application/json
// @Produce	application/json
// @Param		Authorization	header	string	true	"Bearer 用户令牌"
// @Param		id				path	int		true	"id"
// @Param 		{object} body  models.{{.ModelName}} true "body"
// @Success	200
// @Failure 400                        "请求错误"
// @Failure 401                        "token验证失败"
// @Failure 500                         "内部错误"
// @Router		/api/{{.controllerName2Dash}}s/{id} [put]
func {{.ControllerName}}Update(c *gin.Context) {
	id := c.Param(`id`)
	if id == `` {
		response.Error(c,`pls input id`, http.StatusBadRequest)
		return
	}
	model := make(map[string]any)
	err := c.ShouldBindBodyWith(&model,binding.JSON)
	if err != nil {
		response.Error(c, err.Error(), http.StatusBadRequest)
		return
	}
	updated, err := services.{{.ServiceName}}.Update(c, model, cast.ToUint(id))
	if err != nil {
		if errors.Is(err, gorm.ErrRecordNotFound) {
			response.Error(c, err.Error(), http.StatusNotFound,)
			return
		}
		response.Error(c, err.Error(), http.StatusBadRequest)
		return
	}
	response.OK(c,updated)
}

// @Summary	删除(delete)
// @Tags		{{.SwaggerTags}}
// @Accept		application/json
// @Produce	application/json
// @Param		Authorization	header	string	true	"Bearer 用户令牌"
// @Param		id				path	int		true	"id"
// @Success	200
// @Failure 400                        "请求错误"
// @Failure 401                        "token验证失败"
// @Failure 500                         "内部错误"
// @Router		/api/{{.controllerName2Dash}}s/{id} [delete]
func {{.ControllerName}}Del(c *gin.Context) {
	id := c.Param(`id`)
	if id == `` {
		response.Error(c,`pls input id`, http.StatusBadRequest)
		return
	}
	deleted, err := services.{{.ServiceName}}.Delete(c, cast.ToUint(id))
	if err != nil {
		if errors.Is(err, gorm.ErrRecordNotFound) {
			response.Error(c, err.Error(), http.StatusNotFound,)
			return
		}
		response.Error(c, err.Error(), http.StatusBadRequest)
		return
	}
	response.OK(c,deleted)
}
