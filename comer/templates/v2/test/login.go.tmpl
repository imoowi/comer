package test

func DoLogin() string {
	/*
		router := global.SetupRouter()
		w := httptest.NewRecorder()
		userAdd := models.UserAdd{Username: `root`, Passwd: `root`}
		dataByte, _ := json.Marshal(userAdd)
		payload := strings.NewReader(string(dataByte))
		req, _ := http.NewRequest("POST", "/api/auth/login", payload)
		router.ServeHTTP(w, req)
		body, _ := io.ReadAll(w.Body)
		res := make(map[string]string)
		json.Unmarshal(body, &res)
		if token, ok := res[`token`]; ok {
			return token
		}
		fmt.Println(body)
		//*/
	return ``
}
